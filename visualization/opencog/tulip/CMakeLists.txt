ADD_LIBRARY(adaptor-tulip STATIC
    TulipWriter.cc
)

INCLUDE_DIRECTORIES(
    ${ATOMSPACE_INCLUDE_DIR}
    ${COGSERVER_INCLUDE_DIR}
    ${COGUTIL_INCLUDE_DIR}
)

TARGET_LINK_LIBRARIES(adaptor-tulip
    ${COGSERVER_LIBRARIES}
    ${COGUTIL_LIBRARY}
)
