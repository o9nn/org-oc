# optimal-build.yml — Auto-generated by oc-build-optimizer
# Iteration: 25 | Generated: 2026-03-10T09:03:12.983693+00:00
# Critical path: cogutil -> atomspace -> atomspace-storage -> cogserver -> attention -> opencog
# Estimated makespan: 10.0 min | Tiers: 6

name: OC Optimal Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      components:
        description: 'Components to build (empty=all)'
        required: false
        default: ''

env:
  CMAKE_BUILD_TYPE: Release
  MAKEFLAGS: "-j$(nproc)"
  INSTALL_PREFIX: /usr/local

jobs:
  system_setup:
    name: System Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake binutils-dev cmake cxxtest cython3 flex guile-3.0-dev libboost-all-dev libiberty-dev liboctomap-dev libopencv-dev libopenmpi-dev libpqxx-dev libprotobuf-dev librocksdb-dev libssl-dev libtool pkg-config protobuf-compiler swig unixodbc-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig

  cogutil:
    name: 'T0: cogutil'
    runs-on: ubuntu-latest
    needs: [system_setup]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y binutils-dev cmake cxxtest guile-3.0-dev libboost-all-dev libiberty-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - name: Snapshot before cogutil
        run: |
          find /usr/local -type f | sort > /tmp/before-cogutil.txt
      - name: Build cogutil
        run: |
          git clone --depth 1 https://github.com/opencog/cogutil /tmp/cogutil-src || true
          mkdir -p /tmp/cogutil-build && cd /tmp/cogutil-build
          cmake /tmp/cogutil-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package cogutil
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-cogutil.txt
          mkdir -p /tmp/cogutil-pkg
          comm -13 /tmp/before-cogutil.txt /tmp/after-cogutil.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/cogutil-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/cogutil-pkg/$rel"
          done
          du -sh /tmp/cogutil-pkg/ && find /tmp/cogutil-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cogutil-install
          path: /tmp/cogutil-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cogutil-logs
          path: /tmp/cogutil-build/*.log
          retention-days: 7

  link_grammar:
    name: 'T0: link-grammar'
    runs-on: ubuntu-latest
    needs: [system_setup]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake cmake cxxtest flex guile-3.0-dev libboost-all-dev libtool pkg-config swig
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - name: Snapshot before link-grammar
        run: |
          find /usr/local -type f | sort > /tmp/before-link-grammar.txt
      - name: Build link-grammar
        run: |
          git clone --depth 1 https://github.com/opencog/link-grammar /tmp/link-grammar-src || true
          cd /tmp/link-grammar-src
          ./autogen.sh --no-configure 2>&1 | tee /tmp/link-grammar-build/autogen.log || true
          mkdir -p /tmp/link-grammar-build && cd /tmp/link-grammar-build
          /tmp/link-grammar-src/configure --prefix=/usr/local 2>&1 | tee configure.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package link-grammar
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-link-grammar.txt
          mkdir -p /tmp/link-grammar-pkg
          comm -13 /tmp/before-link-grammar.txt /tmp/after-link-grammar.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/link-grammar-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/link-grammar-pkg/$rel"
          done
          du -sh /tmp/link-grammar-pkg/ && find /tmp/link-grammar-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: link_grammar-install
          path: /tmp/link-grammar-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: link_grammar-logs
          path: /tmp/link-grammar-build/*.log
          retention-days: 7

  atomspace:
    name: 'T1: atomspace'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest cython3 guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace.txt
      - name: Build atomspace
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace /tmp/atomspace-src || true
          mkdir -p /tmp/atomspace-build && cd /tmp/atomspace-build
          cmake /tmp/atomspace-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace.txt
          mkdir -p /tmp/atomspace-pkg
          comm -13 /tmp/before-atomspace.txt /tmp/after-atomspace.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-pkg/$rel"
          done
          du -sh /tmp/atomspace-pkg/ && find /tmp/atomspace-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace-install
          path: /tmp/atomspace-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace-logs
          path: /tmp/atomspace-build/*.log
          retention-days: 7

  moses:
    name: 'T1: moses'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopenmpi-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before moses
        run: |
          find /usr/local -type f | sort > /tmp/before-moses.txt
      - name: Build moses
        run: |
          git clone --depth 1 https://github.com/opencog/moses /tmp/moses-src || true
          mkdir -p /tmp/moses-build && cd /tmp/moses-build
          cmake /tmp/moses-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package moses
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-moses.txt
          mkdir -p /tmp/moses-pkg
          comm -13 /tmp/before-moses.txt /tmp/after-moses.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/moses-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/moses-pkg/$rel"
          done
          du -sh /tmp/moses-pkg/ && find /tmp/moses-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: moses-install
          path: /tmp/moses-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moses-logs
          path: /tmp/moses-build/*.log
          retention-days: 7

  agi_bio:
    name: 'T2: agi-bio'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before agi-bio
        run: |
          find /usr/local -type f | sort > /tmp/before-agi-bio.txt
      - name: Build agi-bio
        run: |
          git clone --depth 1 https://github.com/opencog/agi-bio /tmp/agi-bio-src || true
          mkdir -p /tmp/agi-bio-build && cd /tmp/agi-bio-build
          cmake /tmp/agi-bio-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package agi-bio
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-agi-bio.txt
          mkdir -p /tmp/agi-bio-pkg
          comm -13 /tmp/before-agi-bio.txt /tmp/after-agi-bio.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/agi-bio-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/agi-bio-pkg/$rel"
          done
          du -sh /tmp/agi-bio-pkg/ && find /tmp/agi-bio-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: agi_bio-install
          path: /tmp/agi-bio-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agi_bio-logs
          path: /tmp/agi-bio-build/*.log
          retention-days: 7

  atomspace_agents:
    name: 'T2: atomspace-agents'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-agents
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-agents.txt
      - name: Build atomspace-agents
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-agents /tmp/atomspace-agents-src || true
          mkdir -p /tmp/atomspace-agents-build && cd /tmp/atomspace-agents-build
          cmake /tmp/atomspace-agents-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-agents
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-agents.txt
          mkdir -p /tmp/atomspace-agents-pkg
          comm -13 /tmp/before-atomspace-agents.txt /tmp/after-atomspace-agents.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-agents-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-agents-pkg/$rel"
          done
          du -sh /tmp/atomspace-agents-pkg/ && find /tmp/atomspace-agents-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_agents-install
          path: /tmp/atomspace-agents-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_agents-logs
          path: /tmp/atomspace-agents-build/*.log
          retention-days: 7

  atomspace_rpc:
    name: 'T2: atomspace-rpc'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libprotobuf-dev protobuf-compiler
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-rpc
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-rpc.txt
      - name: Build atomspace-rpc
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-rpc /tmp/atomspace-rpc-src || true
          mkdir -p /tmp/atomspace-rpc-build && cd /tmp/atomspace-rpc-build
          cmake /tmp/atomspace-rpc-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-rpc
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-rpc.txt
          mkdir -p /tmp/atomspace-rpc-pkg
          comm -13 /tmp/before-atomspace-rpc.txt /tmp/after-atomspace-rpc.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-rpc-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-rpc-pkg/$rel"
          done
          du -sh /tmp/atomspace-rpc-pkg/ && find /tmp/atomspace-rpc-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_rpc-install
          path: /tmp/atomspace-rpc-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_rpc-logs
          path: /tmp/atomspace-rpc-build/*.log
          retention-days: 7

  atomspace_storage:
    name: 'T2: atomspace-storage'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-storage
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-storage.txt
      - name: Build atomspace-storage
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-storage /tmp/atomspace-storage-src || true
          mkdir -p /tmp/atomspace-storage-build && cd /tmp/atomspace-storage-build
          cmake /tmp/atomspace-storage-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-storage
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-storage.txt
          mkdir -p /tmp/atomspace-storage-pkg
          comm -13 /tmp/before-atomspace-storage.txt /tmp/after-atomspace-storage.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-storage-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-storage-pkg/$rel"
          done
          du -sh /tmp/atomspace-storage-pkg/ && find /tmp/atomspace-storage-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_storage-logs
          path: /tmp/atomspace-storage-build/*.log
          retention-days: 7

  cheminformatics:
    name: 'T2: cheminformatics'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before cheminformatics
        run: |
          find /usr/local -type f | sort > /tmp/before-cheminformatics.txt
      - name: Build cheminformatics
        run: |
          git clone --depth 1 https://github.com/opencog/cheminformatics /tmp/cheminformatics-src || true
          mkdir -p /tmp/cheminformatics-build && cd /tmp/cheminformatics-build
          cmake /tmp/cheminformatics-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package cheminformatics
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-cheminformatics.txt
          mkdir -p /tmp/cheminformatics-pkg
          comm -13 /tmp/before-cheminformatics.txt /tmp/after-cheminformatics.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/cheminformatics-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/cheminformatics-pkg/$rel"
          done
          du -sh /tmp/cheminformatics-pkg/ && find /tmp/cheminformatics-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cheminformatics-install
          path: /tmp/cheminformatics-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cheminformatics-logs
          path: /tmp/cheminformatics-build/*.log
          retention-days: 7

  generate:
    name: 'T2: generate'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before generate
        run: |
          find /usr/local -type f | sort > /tmp/before-generate.txt
      - name: Build generate
        run: |
          git clone --depth 1 https://github.com/opencog/generate /tmp/generate-src || true
          mkdir -p /tmp/generate-build && cd /tmp/generate-build
          cmake /tmp/generate-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package generate
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-generate.txt
          mkdir -p /tmp/generate-pkg
          comm -13 /tmp/before-generate.txt /tmp/after-generate.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/generate-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/generate-pkg/$rel"
          done
          du -sh /tmp/generate-pkg/ && find /tmp/generate-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: generate-install
          path: /tmp/generate-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: generate-logs
          path: /tmp/generate-build/*.log
          retention-days: 7

  matrix:
    name: 'T2: matrix'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before matrix
        run: |
          find /usr/local -type f | sort > /tmp/before-matrix.txt
      - name: Build matrix
        run: |
          git clone --depth 1 https://github.com/opencog/matrix /tmp/matrix-src || true
          mkdir -p /tmp/matrix-build && cd /tmp/matrix-build
          cmake /tmp/matrix-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package matrix
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-matrix.txt
          mkdir -p /tmp/matrix-pkg
          comm -13 /tmp/before-matrix.txt /tmp/after-matrix.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/matrix-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/matrix-pkg/$rel"
          done
          du -sh /tmp/matrix-pkg/ && find /tmp/matrix-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: matrix-install
          path: /tmp/matrix-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: matrix-logs
          path: /tmp/matrix-build/*.log
          retention-days: 7

  pattern_index:
    name: 'T2: pattern-index'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before pattern-index
        run: |
          find /usr/local -type f | sort > /tmp/before-pattern-index.txt
      - name: Build pattern-index
        run: |
          git clone --depth 1 https://github.com/opencog/pattern-index /tmp/pattern-index-src || true
          mkdir -p /tmp/pattern-index-build && cd /tmp/pattern-index-build
          cmake /tmp/pattern-index-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package pattern-index
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-pattern-index.txt
          mkdir -p /tmp/pattern-index-pkg
          comm -13 /tmp/before-pattern-index.txt /tmp/after-pattern-index.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/pattern-index-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/pattern-index-pkg/$rel"
          done
          du -sh /tmp/pattern-index-pkg/ && find /tmp/pattern-index-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: pattern_index-install
          path: /tmp/pattern-index-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pattern_index-logs
          path: /tmp/pattern-index-build/*.log
          retention-days: 7

  sensory:
    name: 'T2: sensory'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before sensory
        run: |
          find /usr/local -type f | sort > /tmp/before-sensory.txt
      - name: Build sensory
        run: |
          git clone --depth 1 https://github.com/opencog/sensory /tmp/sensory-src || true
          mkdir -p /tmp/sensory-build && cd /tmp/sensory-build
          cmake /tmp/sensory-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package sensory
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-sensory.txt
          mkdir -p /tmp/sensory-pkg
          comm -13 /tmp/before-sensory.txt /tmp/after-sensory.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/sensory-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/sensory-pkg/$rel"
          done
          du -sh /tmp/sensory-pkg/ && find /tmp/sensory-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: sensory-install
          path: /tmp/sensory-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sensory-logs
          path: /tmp/sensory-build/*.log
          retention-days: 7

  spacetime:
    name: 'T2: spacetime'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev liboctomap-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before spacetime
        run: |
          find /usr/local -type f | sort > /tmp/before-spacetime.txt
      - name: Build spacetime
        run: |
          git clone --depth 1 https://github.com/opencog/spacetime /tmp/spacetime-src || true
          mkdir -p /tmp/spacetime-build && cd /tmp/spacetime-build
          cmake /tmp/spacetime-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package spacetime
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-spacetime.txt
          mkdir -p /tmp/spacetime-pkg
          comm -13 /tmp/before-spacetime.txt /tmp/after-spacetime.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/spacetime-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/spacetime-pkg/$rel"
          done
          du -sh /tmp/spacetime-pkg/ && find /tmp/spacetime-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: spacetime-install
          path: /tmp/spacetime-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: spacetime-logs
          path: /tmp/spacetime-build/*.log
          retention-days: 7

  unify:
    name: 'T2: unify'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before unify
        run: |
          find /usr/local -type f | sort > /tmp/before-unify.txt
      - name: Build unify
        run: |
          git clone --depth 1 https://github.com/opencog/unify /tmp/unify-src || true
          mkdir -p /tmp/unify-build && cd /tmp/unify-build
          cmake /tmp/unify-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package unify
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-unify.txt
          mkdir -p /tmp/unify-pkg
          comm -13 /tmp/before-unify.txt /tmp/after-unify.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/unify-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/unify-pkg/$rel"
          done
          du -sh /tmp/unify-pkg/ && find /tmp/unify-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: unify-install
          path: /tmp/unify-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: unify-logs
          path: /tmp/unify-build/*.log
          retention-days: 7

  vision:
    name: 'T2: vision'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopencv-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before vision
        run: |
          find /usr/local -type f | sort > /tmp/before-vision.txt
      - name: Build vision
        run: |
          git clone --depth 1 https://github.com/opencog/vision /tmp/vision-src || true
          mkdir -p /tmp/vision-build && cd /tmp/vision-build
          cmake /tmp/vision-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package vision
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-vision.txt
          mkdir -p /tmp/vision-pkg
          comm -13 /tmp/before-vision.txt /tmp/after-vision.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/vision-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/vision-pkg/$rel"
          done
          du -sh /tmp/vision-pkg/ && find /tmp/vision-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: vision-install
          path: /tmp/vision-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vision-logs
          path: /tmp/vision-build/*.log
          retention-days: 7

  atomspace_bridge:
    name: 'T3: atomspace-bridge'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-bridge
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-bridge.txt
      - name: Build atomspace-bridge
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-bridge /tmp/atomspace-bridge-src || true
          mkdir -p /tmp/atomspace-bridge-build && cd /tmp/atomspace-bridge-build
          cmake /tmp/atomspace-bridge-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-bridge
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-bridge.txt
          mkdir -p /tmp/atomspace-bridge-pkg
          comm -13 /tmp/before-atomspace-bridge.txt /tmp/after-atomspace-bridge.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-bridge-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-bridge-pkg/$rel"
          done
          du -sh /tmp/atomspace-bridge-pkg/ && find /tmp/atomspace-bridge-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_bridge-install
          path: /tmp/atomspace-bridge-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_bridge-logs
          path: /tmp/atomspace-bridge-build/*.log
          retention-days: 7

  atomspace_pgres:
    name: 'T3: atomspace-pgres'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libpqxx-dev unixodbc-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-pgres
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-pgres.txt
      - name: Build atomspace-pgres
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-pgres /tmp/atomspace-pgres-src || true
          mkdir -p /tmp/atomspace-pgres-build && cd /tmp/atomspace-pgres-build
          cmake /tmp/atomspace-pgres-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-pgres
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-pgres.txt
          mkdir -p /tmp/atomspace-pgres-pkg
          comm -13 /tmp/before-atomspace-pgres.txt /tmp/after-atomspace-pgres.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-pgres-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-pgres-pkg/$rel"
          done
          du -sh /tmp/atomspace-pgres-pkg/ && find /tmp/atomspace-pgres-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_pgres-install
          path: /tmp/atomspace-pgres-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_pgres-logs
          path: /tmp/atomspace-pgres-build/*.log
          retention-days: 7

  atomspace_rocks:
    name: 'T3: atomspace-rocks'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev librocksdb-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-rocks
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-rocks.txt
      - name: Build atomspace-rocks
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-rocks /tmp/atomspace-rocks-src || true
          mkdir -p /tmp/atomspace-rocks-build && cd /tmp/atomspace-rocks-build
          cmake /tmp/atomspace-rocks-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-rocks
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-rocks.txt
          mkdir -p /tmp/atomspace-rocks-pkg
          comm -13 /tmp/before-atomspace-rocks.txt /tmp/after-atomspace-rocks.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-rocks-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-rocks-pkg/$rel"
          done
          du -sh /tmp/atomspace-rocks-pkg/ && find /tmp/atomspace-rocks-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_rocks-install
          path: /tmp/atomspace-rocks-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_rocks-logs
          path: /tmp/atomspace-rocks-build/*.log
          retention-days: 7

  cogserver:
    name: 'T3: cogserver'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libssl-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before cogserver
        run: |
          find /usr/local -type f | sort > /tmp/before-cogserver.txt
      - name: Build cogserver
        run: |
          git clone --depth 1 https://github.com/opencog/cogserver /tmp/cogserver-src || true
          mkdir -p /tmp/cogserver-build && cd /tmp/cogserver-build
          cmake /tmp/cogserver-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package cogserver
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-cogserver.txt
          mkdir -p /tmp/cogserver-pkg
          comm -13 /tmp/before-cogserver.txt /tmp/after-cogserver.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/cogserver-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/cogserver-pkg/$rel"
          done
          du -sh /tmp/cogserver-pkg/ && find /tmp/cogserver-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cogserver-install
          path: /tmp/cogserver-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cogserver-logs
          path: /tmp/cogserver-build/*.log
          retention-days: 7

  lg_atomese:
    name: 'T3: lg-atomese'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage, link_grammar]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: link_grammar-install
          path: /tmp/link-grammar-install
      - name: Install link-grammar
        run: |
          if [ -d /tmp/link-grammar-install ]; then
            sudo cp -r /tmp/link-grammar-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before lg-atomese
        run: |
          find /usr/local -type f | sort > /tmp/before-lg-atomese.txt
      - name: Build lg-atomese
        run: |
          git clone --depth 1 https://github.com/opencog/lg-atomese /tmp/lg-atomese-src || true
          mkdir -p /tmp/lg-atomese-build && cd /tmp/lg-atomese-build
          cmake /tmp/lg-atomese-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package lg-atomese
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-lg-atomese.txt
          mkdir -p /tmp/lg-atomese-pkg
          comm -13 /tmp/before-lg-atomese.txt /tmp/after-lg-atomese.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/lg-atomese-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/lg-atomese-pkg/$rel"
          done
          du -sh /tmp/lg-atomese-pkg/ && find /tmp/lg-atomese-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: lg_atomese-install
          path: /tmp/lg-atomese-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lg_atomese-logs
          path: /tmp/lg-atomese-build/*.log
          retention-days: 7

  ure:
    name: 'T3: ure'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, unify]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: unify-install
          path: /tmp/unify-install
      - name: Install unify
        run: |
          if [ -d /tmp/unify-install ]; then
            sudo cp -r /tmp/unify-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before ure
        run: |
          find /usr/local -type f | sort > /tmp/before-ure.txt
      - name: Build ure
        run: |
          git clone --depth 1 https://github.com/opencog/ure /tmp/ure-src || true
          mkdir -p /tmp/ure-build && cd /tmp/ure-build
          cmake /tmp/ure-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package ure
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-ure.txt
          mkdir -p /tmp/ure-pkg
          comm -13 /tmp/before-ure.txt /tmp/after-ure.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/ure-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/ure-pkg/$rel"
          done
          du -sh /tmp/ure-pkg/ && find /tmp/ure-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: ure-install
          path: /tmp/ure-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ure-logs
          path: /tmp/ure-build/*.log
          retention-days: 7

  asmoses:
    name: 'T4: asmoses'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopenmpi-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before asmoses
        run: |
          find /usr/local -type f | sort > /tmp/before-asmoses.txt
      - name: Build asmoses
        run: |
          git clone --depth 1 https://github.com/opencog/asmoses /tmp/asmoses-src || true
          mkdir -p /tmp/asmoses-build && cd /tmp/asmoses-build
          cmake /tmp/asmoses-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package asmoses
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-asmoses.txt
          mkdir -p /tmp/asmoses-pkg
          comm -13 /tmp/before-asmoses.txt /tmp/after-asmoses.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/asmoses-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/asmoses-pkg/$rel"
          done
          du -sh /tmp/asmoses-pkg/ && find /tmp/asmoses-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: asmoses-install
          path: /tmp/asmoses-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: asmoses-logs
          path: /tmp/asmoses-build/*.log
          retention-days: 7

  atomspace_cog:
    name: 'T4: atomspace-cog'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-cog
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-cog.txt
      - name: Build atomspace-cog
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-cog /tmp/atomspace-cog-src || true
          mkdir -p /tmp/atomspace-cog-build && cd /tmp/atomspace-cog-build
          cmake /tmp/atomspace-cog-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-cog
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-cog.txt
          mkdir -p /tmp/atomspace-cog-pkg
          comm -13 /tmp/before-atomspace-cog.txt /tmp/after-atomspace-cog.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-cog-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-cog-pkg/$rel"
          done
          du -sh /tmp/atomspace-cog-pkg/ && find /tmp/atomspace-cog-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_cog-install
          path: /tmp/atomspace-cog-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_cog-logs
          path: /tmp/atomspace-cog-build/*.log
          retention-days: 7

  atomspace_restful:
    name: 'T4: atomspace-restful'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before atomspace-restful
        run: |
          find /usr/local -type f | sort > /tmp/before-atomspace-restful.txt
      - name: Build atomspace-restful
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-restful /tmp/atomspace-restful-src || true
          mkdir -p /tmp/atomspace-restful-build && cd /tmp/atomspace-restful-build
          cmake /tmp/atomspace-restful-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package atomspace-restful
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-atomspace-restful.txt
          mkdir -p /tmp/atomspace-restful-pkg
          comm -13 /tmp/before-atomspace-restful.txt /tmp/after-atomspace-restful.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/atomspace-restful-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/atomspace-restful-pkg/$rel"
          done
          du -sh /tmp/atomspace-restful-pkg/ && find /tmp/atomspace-restful-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_restful-install
          path: /tmp/atomspace-restful-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_restful-logs
          path: /tmp/atomspace-restful-build/*.log
          retention-days: 7

  attention:
    name: 'T4: attention'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before attention
        run: |
          find /usr/local -type f | sort > /tmp/before-attention.txt
      - name: Build attention
        run: |
          git clone --depth 1 https://github.com/opencog/attention /tmp/attention-src || true
          mkdir -p /tmp/attention-build && cd /tmp/attention-build
          cmake /tmp/attention-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package attention
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-attention.txt
          mkdir -p /tmp/attention-pkg
          comm -13 /tmp/before-attention.txt /tmp/after-attention.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/attention-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/attention-pkg/$rel"
          done
          du -sh /tmp/attention-pkg/ && find /tmp/attention-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: attention-install
          path: /tmp/attention-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: attention-logs
          path: /tmp/attention-build/*.log
          retention-days: 7

  learn:
    name: 'T4: learn'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before learn
        run: |
          find /usr/local -type f | sort > /tmp/before-learn.txt
      - name: Build learn
        run: |
          git clone --depth 1 https://github.com/opencog/learn /tmp/learn-src || true
          mkdir -p /tmp/learn-build && cd /tmp/learn-build
          cmake /tmp/learn-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package learn
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-learn.txt
          mkdir -p /tmp/learn-pkg
          comm -13 /tmp/before-learn.txt /tmp/after-learn.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/learn-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/learn-pkg/$rel"
          done
          du -sh /tmp/learn-pkg/ && find /tmp/learn-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: learn-install
          path: /tmp/learn-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: learn-logs
          path: /tmp/learn-build/*.log
          retention-days: 7

  miner:
    name: 'T4: miner'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure, unify]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: unify-install
          path: /tmp/unify-install
      - name: Install unify
        run: |
          if [ -d /tmp/unify-install ]; then
            sudo cp -r /tmp/unify-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before miner
        run: |
          find /usr/local -type f | sort > /tmp/before-miner.txt
      - name: Build miner
        run: |
          git clone --depth 1 https://github.com/opencog/miner /tmp/miner-src || true
          mkdir -p /tmp/miner-build && cd /tmp/miner-build
          cmake /tmp/miner-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package miner
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-miner.txt
          mkdir -p /tmp/miner-pkg
          comm -13 /tmp/before-miner.txt /tmp/after-miner.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/miner-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/miner-pkg/$rel"
          done
          du -sh /tmp/miner-pkg/ && find /tmp/miner-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: miner-install
          path: /tmp/miner-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: miner-logs
          path: /tmp/miner-build/*.log
          retention-days: 7

  pln:
    name: 'T4: pln'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure, spacetime]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: spacetime-install
          path: /tmp/spacetime-install
      - name: Install spacetime
        run: |
          if [ -d /tmp/spacetime-install ]; then
            sudo cp -r /tmp/spacetime-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before pln
        run: |
          find /usr/local -type f | sort > /tmp/before-pln.txt
      - name: Build pln
        run: |
          git clone --depth 1 https://github.com/opencog/pln /tmp/pln-src || true
          mkdir -p /tmp/pln-build && cd /tmp/pln-build
          cmake /tmp/pln-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package pln
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-pln.txt
          mkdir -p /tmp/pln-pkg
          comm -13 /tmp/before-pln.txt /tmp/after-pln.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/pln-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/pln-pkg/$rel"
          done
          du -sh /tmp/pln-pkg/ && find /tmp/pln-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: pln-install
          path: /tmp/pln-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pln-logs
          path: /tmp/pln-build/*.log
          retention-days: 7

  visualization:
    name: 'T4: visualization'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before visualization
        run: |
          find /usr/local -type f | sort > /tmp/before-visualization.txt
      - name: Build visualization
        run: |
          git clone --depth 1 https://github.com/opencog/visualization /tmp/visualization-src || true
          mkdir -p /tmp/visualization-build && cd /tmp/visualization-build
          cmake /tmp/visualization-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package visualization
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-visualization.txt
          mkdir -p /tmp/visualization-pkg
          comm -13 /tmp/before-visualization.txt /tmp/after-visualization.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/visualization-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/visualization-pkg/$rel"
          done
          du -sh /tmp/visualization-pkg/ && find /tmp/visualization-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: visualization-install
          path: /tmp/visualization-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: visualization-logs
          path: /tmp/visualization-build/*.log
          retention-days: 7

  dimensional_embedding:
    name: 'T5: dimensional-embedding'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, attention]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before dimensional-embedding
        run: |
          find /usr/local -type f | sort > /tmp/before-dimensional-embedding.txt
      - name: Build dimensional-embedding
        run: |
          git clone --depth 1 https://github.com/opencog/dimensional-embedding /tmp/dimensional-embedding-src || true
          mkdir -p /tmp/dimensional-embedding-build && cd /tmp/dimensional-embedding-build
          cmake /tmp/dimensional-embedding-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package dimensional-embedding
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-dimensional-embedding.txt
          mkdir -p /tmp/dimensional-embedding-pkg
          comm -13 /tmp/before-dimensional-embedding.txt /tmp/after-dimensional-embedding.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/dimensional-embedding-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/dimensional-embedding-pkg/$rel"
          done
          du -sh /tmp/dimensional-embedding-pkg/ && find /tmp/dimensional-embedding-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: dimensional_embedding-install
          path: /tmp/dimensional-embedding-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dimensional_embedding-logs
          path: /tmp/dimensional-embedding-build/*.log
          retention-days: 7

  opencog:
    name: 'T5: opencog'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, attention, ure, pln, lg_atomese]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: pln-install
          path: /tmp/pln-install
      - name: Install pln
        run: |
          if [ -d /tmp/pln-install ]; then
            sudo cp -r /tmp/pln-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: lg_atomese-install
          path: /tmp/lg-atomese-install
      - name: Install lg-atomese
        run: |
          if [ -d /tmp/lg-atomese-install ]; then
            sudo cp -r /tmp/lg-atomese-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before opencog
        run: |
          find /usr/local -type f | sort > /tmp/before-opencog.txt
      - name: Build opencog
        run: |
          git clone --depth 1 https://github.com/opencog/opencog /tmp/opencog-src || true
          mkdir -p /tmp/opencog-build && cd /tmp/opencog-build
          cmake /tmp/opencog-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package opencog
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-opencog.txt
          mkdir -p /tmp/opencog-pkg
          comm -13 /tmp/before-opencog.txt /tmp/after-opencog.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/opencog-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/opencog-pkg/$rel"
          done
          du -sh /tmp/opencog-pkg/ && find /tmp/opencog-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: opencog-install
          path: /tmp/opencog-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: opencog-logs
          path: /tmp/opencog-build/*.log
          retention-days: 7

  python_attic:
    name: 'T5: python-attic'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, ure, pln, attention]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: pln-install
          path: /tmp/pln-install
      - name: Install pln
        run: |
          if [ -d /tmp/pln-install ]; then
            sudo cp -r /tmp/pln-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Snapshot before python-attic
        run: |
          find /usr/local -type f | sort > /tmp/before-python-attic.txt
      - name: Build python-attic
        run: |
          git clone --depth 1 https://github.com/opencog/python-attic /tmp/python-attic-src || true
          mkdir -p /tmp/python-attic-build && cd /tmp/python-attic-build
          cmake /tmp/python-attic-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install 2>&1 | tee install.log
          sudo ldconfig
      - name: Package python-attic
        if: success()
        run: |
          find /usr/local -type f | sort > /tmp/after-python-attic.txt
          mkdir -p /tmp/python-attic-pkg
          comm -13 /tmp/before-python-attic.txt /tmp/after-python-attic.txt | while read f; do
            rel=${f#/usr/local/}
            mkdir -p /tmp/python-attic-pkg/$(dirname $rel)
            cp -a "$f" "/tmp/python-attic-pkg/$rel"
          done
          du -sh /tmp/python-attic-pkg/ && find /tmp/python-attic-pkg -type f | wc -l
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: python_attic-install
          path: /tmp/python-attic-pkg
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python_attic-logs
          path: /tmp/python-attic-build/*.log
          retention-days: 7

