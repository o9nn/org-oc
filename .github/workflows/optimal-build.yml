# optimal-build.yml — Auto-generated by oc-build-optimizer
# Iteration: 19 | Generated: 2026-03-10T08:11:16.648057+00:00
# Critical path: cogutil -> atomspace -> atomspace-storage -> cogserver -> attention -> opencog
# Estimated makespan: 10.0 min | Tiers: 6

name: OC Optimal Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      components:
        description: 'Components to build (empty=all)'
        required: false
        default: ''

env:
  CMAKE_BUILD_TYPE: Release
  MAKEFLAGS: "-j$(nproc)"
  INSTALL_PREFIX: /usr/local

jobs:
  system_setup:
    name: System Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake binutils-dev cmake cxxtest cython3 flex guile-3.0-dev libboost-all-dev libiberty-dev liboctomap-dev libopencv-dev libopenmpi-dev libpqxx-dev libprotobuf-dev librocksdb-dev libssl-dev libtool pkg-config protobuf-compiler swig unixodbc-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig

  cogutil:
    name: 'T0: cogutil'
    runs-on: ubuntu-latest
    needs: [system_setup]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y binutils-dev cmake cxxtest guile-3.0-dev libboost-all-dev libiberty-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - name: Build cogutil
        run: |
          git clone --depth 1 https://github.com/opencog/cogutil /tmp/cogutil-src || true
          mkdir -p /tmp/cogutil-build && cd /tmp/cogutil-build
          cmake /tmp/cogutil-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/cogutil-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect cogutil install
        if: success()
        run: |
          mkdir -p /tmp/cogutil-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cogutil-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cogutil-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cogutil-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cogutil-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/cogutil-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cogutil-install
          path: /tmp/cogutil-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cogutil-logs
          path: /tmp/cogutil-build/*.log
          retention-days: 7

  link_grammar:
    name: 'T0: link-grammar'
    runs-on: ubuntu-latest
    needs: [system_setup]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake cmake cxxtest flex guile-3.0-dev libboost-all-dev libtool pkg-config swig
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - name: Build link-grammar
        run: |
          git clone --depth 1 https://github.com/opencog/link-grammar /tmp/link-grammar-src || true
          mkdir -p /tmp/link-grammar-build && cd /tmp/link-grammar-build
          cmake /tmp/link-grammar-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/link-grammar-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect link-grammar install
        if: success()
        run: |
          mkdir -p /tmp/link-grammar-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/link-grammar-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/link-grammar-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/link-grammar-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/link-grammar-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/link-grammar-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: link_grammar-install
          path: /tmp/link-grammar-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: link_grammar-logs
          path: /tmp/link-grammar-build/*.log
          retention-days: 7

  atomspace:
    name: 'T1: atomspace'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest cython3 guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace /tmp/atomspace-src || true
          mkdir -p /tmp/atomspace-build && cd /tmp/atomspace-build
          cmake /tmp/atomspace-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace-install
          path: /tmp/atomspace-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace-logs
          path: /tmp/atomspace-build/*.log
          retention-days: 7

  moses:
    name: 'T1: moses'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopenmpi-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build moses
        run: |
          git clone --depth 1 https://github.com/opencog/moses /tmp/moses-src || true
          mkdir -p /tmp/moses-build && cd /tmp/moses-build
          cmake /tmp/moses-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/moses-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect moses install
        if: success()
        run: |
          mkdir -p /tmp/moses-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/moses-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/moses-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/moses-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/moses-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/moses-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: moses-install
          path: /tmp/moses-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moses-logs
          path: /tmp/moses-build/*.log
          retention-days: 7

  agi_bio:
    name: 'T2: agi-bio'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build agi-bio
        run: |
          git clone --depth 1 https://github.com/opencog/agi-bio /tmp/agi-bio-src || true
          mkdir -p /tmp/agi-bio-build && cd /tmp/agi-bio-build
          cmake /tmp/agi-bio-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/agi-bio-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect agi-bio install
        if: success()
        run: |
          mkdir -p /tmp/agi-bio-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/agi-bio-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/agi-bio-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/agi-bio-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/agi-bio-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/agi-bio-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: agi_bio-install
          path: /tmp/agi-bio-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agi_bio-logs
          path: /tmp/agi-bio-build/*.log
          retention-days: 7

  atomspace_agents:
    name: 'T2: atomspace-agents'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-agents
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-agents /tmp/atomspace-agents-src || true
          mkdir -p /tmp/atomspace-agents-build && cd /tmp/atomspace-agents-build
          cmake /tmp/atomspace-agents-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-agents-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-agents install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-agents-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-agents-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-agents-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-agents-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-agents-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-agents-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_agents-install
          path: /tmp/atomspace-agents-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_agents-logs
          path: /tmp/atomspace-agents-build/*.log
          retention-days: 7

  atomspace_rpc:
    name: 'T2: atomspace-rpc'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libprotobuf-dev protobuf-compiler
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-rpc
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-rpc /tmp/atomspace-rpc-src || true
          mkdir -p /tmp/atomspace-rpc-build && cd /tmp/atomspace-rpc-build
          cmake /tmp/atomspace-rpc-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-rpc-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-rpc install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-rpc-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-rpc-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-rpc-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-rpc-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-rpc-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-rpc-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_rpc-install
          path: /tmp/atomspace-rpc-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_rpc-logs
          path: /tmp/atomspace-rpc-build/*.log
          retention-days: 7

  atomspace_storage:
    name: 'T2: atomspace-storage'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-storage
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-storage /tmp/atomspace-storage-src || true
          mkdir -p /tmp/atomspace-storage-build && cd /tmp/atomspace-storage-build
          cmake /tmp/atomspace-storage-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-storage-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-storage install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-storage-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-storage-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-storage-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-storage-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-storage-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-storage-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_storage-logs
          path: /tmp/atomspace-storage-build/*.log
          retention-days: 7

  cheminformatics:
    name: 'T2: cheminformatics'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build cheminformatics
        run: |
          git clone --depth 1 https://github.com/opencog/cheminformatics /tmp/cheminformatics-src || true
          mkdir -p /tmp/cheminformatics-build && cd /tmp/cheminformatics-build
          cmake /tmp/cheminformatics-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/cheminformatics-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect cheminformatics install
        if: success()
        run: |
          mkdir -p /tmp/cheminformatics-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cheminformatics-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cheminformatics-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cheminformatics-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cheminformatics-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/cheminformatics-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cheminformatics-install
          path: /tmp/cheminformatics-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cheminformatics-logs
          path: /tmp/cheminformatics-build/*.log
          retention-days: 7

  generate:
    name: 'T2: generate'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build generate
        run: |
          git clone --depth 1 https://github.com/opencog/generate /tmp/generate-src || true
          mkdir -p /tmp/generate-build && cd /tmp/generate-build
          cmake /tmp/generate-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/generate-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect generate install
        if: success()
        run: |
          mkdir -p /tmp/generate-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/generate-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/generate-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/generate-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/generate-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/generate-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: generate-install
          path: /tmp/generate-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: generate-logs
          path: /tmp/generate-build/*.log
          retention-days: 7

  matrix:
    name: 'T2: matrix'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build matrix
        run: |
          git clone --depth 1 https://github.com/opencog/matrix /tmp/matrix-src || true
          mkdir -p /tmp/matrix-build && cd /tmp/matrix-build
          cmake /tmp/matrix-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/matrix-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect matrix install
        if: success()
        run: |
          mkdir -p /tmp/matrix-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/matrix-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/matrix-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/matrix-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/matrix-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/matrix-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: matrix-install
          path: /tmp/matrix-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: matrix-logs
          path: /tmp/matrix-build/*.log
          retention-days: 7

  pattern_index:
    name: 'T2: pattern-index'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build pattern-index
        run: |
          git clone --depth 1 https://github.com/opencog/pattern-index /tmp/pattern-index-src || true
          mkdir -p /tmp/pattern-index-build && cd /tmp/pattern-index-build
          cmake /tmp/pattern-index-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/pattern-index-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect pattern-index install
        if: success()
        run: |
          mkdir -p /tmp/pattern-index-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/pattern-index-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/pattern-index-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/pattern-index-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/pattern-index-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/pattern-index-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: pattern_index-install
          path: /tmp/pattern-index-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pattern_index-logs
          path: /tmp/pattern-index-build/*.log
          retention-days: 7

  sensory:
    name: 'T2: sensory'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build sensory
        run: |
          git clone --depth 1 https://github.com/opencog/sensory /tmp/sensory-src || true
          mkdir -p /tmp/sensory-build && cd /tmp/sensory-build
          cmake /tmp/sensory-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/sensory-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect sensory install
        if: success()
        run: |
          mkdir -p /tmp/sensory-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/sensory-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/sensory-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/sensory-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/sensory-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/sensory-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: sensory-install
          path: /tmp/sensory-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sensory-logs
          path: /tmp/sensory-build/*.log
          retention-days: 7

  spacetime:
    name: 'T2: spacetime'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev liboctomap-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build spacetime
        run: |
          git clone --depth 1 https://github.com/opencog/spacetime /tmp/spacetime-src || true
          mkdir -p /tmp/spacetime-build && cd /tmp/spacetime-build
          cmake /tmp/spacetime-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/spacetime-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect spacetime install
        if: success()
        run: |
          mkdir -p /tmp/spacetime-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/spacetime-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/spacetime-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/spacetime-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/spacetime-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/spacetime-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: spacetime-install
          path: /tmp/spacetime-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: spacetime-logs
          path: /tmp/spacetime-build/*.log
          retention-days: 7

  unify:
    name: 'T2: unify'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build unify
        run: |
          git clone --depth 1 https://github.com/opencog/unify /tmp/unify-src || true
          mkdir -p /tmp/unify-build && cd /tmp/unify-build
          cmake /tmp/unify-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/unify-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect unify install
        if: success()
        run: |
          mkdir -p /tmp/unify-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/unify-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/unify-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/unify-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/unify-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/unify-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: unify-install
          path: /tmp/unify-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: unify-logs
          path: /tmp/unify-build/*.log
          retention-days: 7

  vision:
    name: 'T2: vision'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopencv-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build vision
        run: |
          git clone --depth 1 https://github.com/opencog/vision /tmp/vision-src || true
          mkdir -p /tmp/vision-build && cd /tmp/vision-build
          cmake /tmp/vision-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/vision-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect vision install
        if: success()
        run: |
          mkdir -p /tmp/vision-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/vision-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/vision-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/vision-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/vision-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/vision-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: vision-install
          path: /tmp/vision-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vision-logs
          path: /tmp/vision-build/*.log
          retention-days: 7

  atomspace_bridge:
    name: 'T3: atomspace-bridge'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-bridge
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-bridge /tmp/atomspace-bridge-src || true
          mkdir -p /tmp/atomspace-bridge-build && cd /tmp/atomspace-bridge-build
          cmake /tmp/atomspace-bridge-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-bridge-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-bridge install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-bridge-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-bridge-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-bridge-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-bridge-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-bridge-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-bridge-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_bridge-install
          path: /tmp/atomspace-bridge-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_bridge-logs
          path: /tmp/atomspace-bridge-build/*.log
          retention-days: 7

  atomspace_pgres:
    name: 'T3: atomspace-pgres'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libpqxx-dev unixodbc-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-pgres
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-pgres /tmp/atomspace-pgres-src || true
          mkdir -p /tmp/atomspace-pgres-build && cd /tmp/atomspace-pgres-build
          cmake /tmp/atomspace-pgres-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-pgres-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-pgres install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-pgres-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-pgres-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-pgres-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-pgres-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-pgres-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-pgres-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_pgres-install
          path: /tmp/atomspace-pgres-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_pgres-logs
          path: /tmp/atomspace-pgres-build/*.log
          retention-days: 7

  atomspace_rocks:
    name: 'T3: atomspace-rocks'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev librocksdb-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-rocks
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-rocks /tmp/atomspace-rocks-src || true
          mkdir -p /tmp/atomspace-rocks-build && cd /tmp/atomspace-rocks-build
          cmake /tmp/atomspace-rocks-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-rocks-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-rocks install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-rocks-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-rocks-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-rocks-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-rocks-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-rocks-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-rocks-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_rocks-install
          path: /tmp/atomspace-rocks-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_rocks-logs
          path: /tmp/atomspace-rocks-build/*.log
          retention-days: 7

  cogserver:
    name: 'T3: cogserver'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libssl-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build cogserver
        run: |
          git clone --depth 1 https://github.com/opencog/cogserver /tmp/cogserver-src || true
          mkdir -p /tmp/cogserver-build && cd /tmp/cogserver-build
          cmake /tmp/cogserver-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/cogserver-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect cogserver install
        if: success()
        run: |
          mkdir -p /tmp/cogserver-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cogserver-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cogserver-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/cogserver-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/cogserver-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/cogserver-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: cogserver-install
          path: /tmp/cogserver-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cogserver-logs
          path: /tmp/cogserver-build/*.log
          retention-days: 7

  lg_atomese:
    name: 'T3: lg-atomese'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, atomspace_storage, link_grammar]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev uuid-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: link_grammar-install
          path: /tmp/link-grammar-install
      - name: Install link-grammar
        run: |
          if [ -d /tmp/link-grammar-install ]; then
            sudo cp -r /tmp/link-grammar-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build lg-atomese
        run: |
          git clone --depth 1 https://github.com/opencog/lg-atomese /tmp/lg-atomese-src || true
          mkdir -p /tmp/lg-atomese-build && cd /tmp/lg-atomese-build
          cmake /tmp/lg-atomese-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/lg-atomese-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect lg-atomese install
        if: success()
        run: |
          mkdir -p /tmp/lg-atomese-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/lg-atomese-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/lg-atomese-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/lg-atomese-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/lg-atomese-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/lg-atomese-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: lg_atomese-install
          path: /tmp/lg-atomese-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lg_atomese-logs
          path: /tmp/lg-atomese-build/*.log
          retention-days: 7

  ure:
    name: 'T3: ure'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, unify]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: unify-install
          path: /tmp/unify-install
      - name: Install unify
        run: |
          if [ -d /tmp/unify-install ]; then
            sudo cp -r /tmp/unify-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build ure
        run: |
          git clone --depth 1 https://github.com/opencog/ure /tmp/ure-src || true
          mkdir -p /tmp/ure-build && cd /tmp/ure-build
          cmake /tmp/ure-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/ure-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect ure install
        if: success()
        run: |
          mkdir -p /tmp/ure-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/ure-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/ure-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/ure-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/ure-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/ure-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: ure-install
          path: /tmp/ure-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ure-logs
          path: /tmp/ure-build/*.log
          retention-days: 7

  asmoses:
    name: 'T4: asmoses'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev libopenmpi-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build asmoses
        run: |
          git clone --depth 1 https://github.com/opencog/asmoses /tmp/asmoses-src || true
          mkdir -p /tmp/asmoses-build && cd /tmp/asmoses-build
          cmake /tmp/asmoses-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/asmoses-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect asmoses install
        if: success()
        run: |
          mkdir -p /tmp/asmoses-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/asmoses-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/asmoses-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/asmoses-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/asmoses-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/asmoses-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: asmoses-install
          path: /tmp/asmoses-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: asmoses-logs
          path: /tmp/asmoses-build/*.log
          retention-days: 7

  atomspace_cog:
    name: 'T4: atomspace-cog'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-cog
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-cog /tmp/atomspace-cog-src || true
          mkdir -p /tmp/atomspace-cog-build && cd /tmp/atomspace-cog-build
          cmake /tmp/atomspace-cog-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-cog-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-cog install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-cog-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-cog-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-cog-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-cog-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-cog-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-cog-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_cog-install
          path: /tmp/atomspace-cog-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_cog-logs
          path: /tmp/atomspace-cog-build/*.log
          retention-days: 7

  atomspace_restful:
    name: 'T4: atomspace-restful'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build atomspace-restful
        run: |
          git clone --depth 1 https://github.com/opencog/atomspace-restful /tmp/atomspace-restful-src || true
          mkdir -p /tmp/atomspace-restful-build && cd /tmp/atomspace-restful-build
          cmake /tmp/atomspace-restful-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/atomspace-restful-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect atomspace-restful install
        if: success()
        run: |
          mkdir -p /tmp/atomspace-restful-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-restful-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-restful-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/atomspace-restful-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/atomspace-restful-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/atomspace-restful-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: atomspace_restful-install
          path: /tmp/atomspace-restful-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atomspace_restful-logs
          path: /tmp/atomspace-restful-build/*.log
          retention-days: 7

  attention:
    name: 'T4: attention'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build attention
        run: |
          git clone --depth 1 https://github.com/opencog/attention /tmp/attention-src || true
          mkdir -p /tmp/attention-build && cd /tmp/attention-build
          cmake /tmp/attention-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/attention-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect attention install
        if: success()
        run: |
          mkdir -p /tmp/attention-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/attention-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/attention-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/attention-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/attention-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/attention-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: attention-install
          path: /tmp/attention-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: attention-logs
          path: /tmp/attention-build/*.log
          retention-days: 7

  learn:
    name: 'T4: learn'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, atomspace_storage]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace_storage-install
          path: /tmp/atomspace-storage-install
      - name: Install atomspace-storage
        run: |
          if [ -d /tmp/atomspace-storage-install ]; then
            sudo cp -r /tmp/atomspace-storage-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build learn
        run: |
          git clone --depth 1 https://github.com/opencog/learn /tmp/learn-src || true
          mkdir -p /tmp/learn-build && cd /tmp/learn-build
          cmake /tmp/learn-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/learn-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect learn install
        if: success()
        run: |
          mkdir -p /tmp/learn-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/learn-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/learn-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/learn-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/learn-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/learn-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: learn-install
          path: /tmp/learn-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: learn-logs
          path: /tmp/learn-build/*.log
          retention-days: 7

  miner:
    name: 'T4: miner'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure, unify]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: unify-install
          path: /tmp/unify-install
      - name: Install unify
        run: |
          if [ -d /tmp/unify-install ]; then
            sudo cp -r /tmp/unify-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build miner
        run: |
          git clone --depth 1 https://github.com/opencog/miner /tmp/miner-src || true
          mkdir -p /tmp/miner-build && cd /tmp/miner-build
          cmake /tmp/miner-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/miner-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect miner install
        if: success()
        run: |
          mkdir -p /tmp/miner-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/miner-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/miner-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/miner-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/miner-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/miner-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: miner-install
          path: /tmp/miner-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: miner-logs
          path: /tmp/miner-build/*.log
          retention-days: 7

  pln:
    name: 'T4: pln'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, ure, spacetime]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: spacetime-install
          path: /tmp/spacetime-install
      - name: Install spacetime
        run: |
          if [ -d /tmp/spacetime-install ]; then
            sudo cp -r /tmp/spacetime-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build pln
        run: |
          git clone --depth 1 https://github.com/opencog/pln /tmp/pln-src || true
          mkdir -p /tmp/pln-build && cd /tmp/pln-build
          cmake /tmp/pln-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/pln-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect pln install
        if: success()
        run: |
          mkdir -p /tmp/pln-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/pln-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/pln-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/pln-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/pln-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/pln-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: pln-install
          path: /tmp/pln-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pln-logs
          path: /tmp/pln-build/*.log
          retention-days: 7

  visualization:
    name: 'T4: visualization'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build visualization
        run: |
          git clone --depth 1 https://github.com/opencog/visualization /tmp/visualization-src || true
          mkdir -p /tmp/visualization-build && cd /tmp/visualization-build
          cmake /tmp/visualization-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/visualization-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect visualization install
        if: success()
        run: |
          mkdir -p /tmp/visualization-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/visualization-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/visualization-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/visualization-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/visualization-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/visualization-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: visualization-install
          path: /tmp/visualization-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: visualization-logs
          path: /tmp/visualization-build/*.log
          retention-days: 7

  dimensional_embedding:
    name: 'T5: dimensional-embedding'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, attention]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build dimensional-embedding
        run: |
          git clone --depth 1 https://github.com/opencog/dimensional-embedding /tmp/dimensional-embedding-src || true
          mkdir -p /tmp/dimensional-embedding-build && cd /tmp/dimensional-embedding-build
          cmake /tmp/dimensional-embedding-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/dimensional-embedding-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect dimensional-embedding install
        if: success()
        run: |
          mkdir -p /tmp/dimensional-embedding-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/dimensional-embedding-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/dimensional-embedding-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/dimensional-embedding-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/dimensional-embedding-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/dimensional-embedding-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: dimensional_embedding-install
          path: /tmp/dimensional-embedding-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dimensional_embedding-logs
          path: /tmp/dimensional-embedding-build/*.log
          retention-days: 7

  opencog:
    name: 'T5: opencog'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, attention, ure, pln, lg_atomese]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: pln-install
          path: /tmp/pln-install
      - name: Install pln
        run: |
          if [ -d /tmp/pln-install ]; then
            sudo cp -r /tmp/pln-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: lg_atomese-install
          path: /tmp/lg-atomese-install
      - name: Install lg-atomese
        run: |
          if [ -d /tmp/lg-atomese-install ]; then
            sudo cp -r /tmp/lg-atomese-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build opencog
        run: |
          git clone --depth 1 https://github.com/opencog/opencog /tmp/opencog-src || true
          mkdir -p /tmp/opencog-build && cd /tmp/opencog-build
          cmake /tmp/opencog-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/opencog-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect opencog install
        if: success()
        run: |
          mkdir -p /tmp/opencog-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/opencog-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/opencog-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/opencog-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/opencog-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/opencog-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: opencog-install
          path: /tmp/opencog-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: opencog-logs
          path: /tmp/opencog-build/*.log
          retention-days: 7

  python_attic:
    name: 'T5: python-attic'
    runs-on: ubuntu-latest
    needs: [system_setup, cogutil, atomspace, cogserver, ure, pln, attention]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake cxxtest guile-3.0-dev libboost-all-dev
          echo "/usr/local/lib/opencog" | sudo tee /etc/ld.so.conf.d/opencog.conf
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogutil-install
          path: /tmp/cogutil-install
      - name: Install cogutil
        run: |
          if [ -d /tmp/cogutil-install ]; then
            sudo cp -r /tmp/cogutil-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: atomspace-install
          path: /tmp/atomspace-install
      - name: Install atomspace
        run: |
          if [ -d /tmp/atomspace-install ]; then
            sudo cp -r /tmp/atomspace-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: cogserver-install
          path: /tmp/cogserver-install
      - name: Install cogserver
        run: |
          if [ -d /tmp/cogserver-install ]; then
            sudo cp -r /tmp/cogserver-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: ure-install
          path: /tmp/ure-install
      - name: Install ure
        run: |
          if [ -d /tmp/ure-install ]; then
            sudo cp -r /tmp/ure-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: pln-install
          path: /tmp/pln-install
      - name: Install pln
        run: |
          if [ -d /tmp/pln-install ]; then
            sudo cp -r /tmp/pln-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - uses: actions/download-artifact@v4
        with:
          name: attention-install
          path: /tmp/attention-install
      - name: Install attention
        run: |
          if [ -d /tmp/attention-install ]; then
            sudo cp -r /tmp/attention-install/* /usr/local/ 2>/dev/null || true
          fi
          sudo ldconfig
      - name: Build python-attic
        run: |
          git clone --depth 1 https://github.com/opencog/python-attic /tmp/python-attic-src || true
          mkdir -p /tmp/python-attic-build && cd /tmp/python-attic-build
          cmake /tmp/python-attic-src -DCMAKE_INSTALL_PREFIX=$INSTALL_PREFIX -DCMAKE_BUILD_TYPE=$CMAKE_BUILD_TYPE 2>&1 | tee cmake.log
          make $MAKEFLAGS 2>&1 | tee make.log
          sudo make install DESTDIR=/tmp/python-attic-dest 2>&1 | tee install.log
          sudo ldconfig
      - name: Collect python-attic install
        if: success()
        run: |
          mkdir -p /tmp/python-attic-artifact
          # Copy only opencog headers and libs, not entire /usr/local
          for d in include/opencog lib/opencog lib/cmake lib/pkgconfig share/opencog lib/libcogutil* lib/libatomspace* lib/libcogserver* lib/libsmob* lib/libpersist* lib/libvalue*; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/python-attic-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/python-attic-artifact/$d 2>/dev/null || true
          done
          # Also capture guile modules
          for d in share/guile lib/guile; do
            [ -e /usr/local/$d ] && mkdir -p /tmp/python-attic-artifact/$(dirname $d) && cp -r /usr/local/$d /tmp/python-attic-artifact/$d 2>/dev/null || true
          done
          du -sh /tmp/python-attic-artifact/ || true
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: python_attic-install
          path: /tmp/python-attic-artifact
          retention-days: 1
          compression-level: 9
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python_attic-logs
          path: /tmp/python-attic-build/*.log
          retention-days: 7

